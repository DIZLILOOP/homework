# Конфигурация базы данных
const DB_HOST = "localhost"
const DB_PORT = 5432
const DB_NAME = "mydatabase"

const CONNECTION_POOL = {
    MIN_SIZE: 5,
    MAX_SIZE: 20,
    TIMEOUT: 30.5
}

const REPLICAS = array("replica1", "replica2", "replica3")